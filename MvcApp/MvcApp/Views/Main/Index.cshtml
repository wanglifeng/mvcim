@model List<MvcApp.Models.QueryModels>

<table class="table table-striped table-bordered table-hover table-condensed">
    <tbody>
        <tr>
            <th>腹围</th>
            <th>姓名</th>
            <th>所在部门</th>
            <th>用户状态</th>s
            <th>用户类型</th>
            <th>操作</th>
        </tr>
        @foreach (var t in Model)
        {
            <tr>
                <td>@t.abd</td>
            </tr>
        }
    </tbody>
</table>
